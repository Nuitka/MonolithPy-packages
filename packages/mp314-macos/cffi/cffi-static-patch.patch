--- a/setup.py
+++ b/setup.py
@@ -150,13 +150,16 @@
         sources.extend(os.path.join(COMPILE_LIBFFI, filename)
                     for filename in _filenames)
 else:
-    use_pkg_config()
+    #use_pkg_config()
+    import __mp__
+    include_dirs.append(__mp__.find_dep_include('base'))
+    library_dirs.append(__mp__.find_dep_libs('base'))
     ask_supports_thread()
     ask_supports_sync_synchronize()
 
-if sysconfig.get_platform().startswith('macosx'):
-    # priority is given to `pkg_config`, but always fall back on SDK's libffi.
-    extra_compile_args += ['-iwithsysroot/usr/include/ffi']
+#if sysconfig.get_platform().startswith('macosx'):
+#    # priority is given to `pkg_config`, but always fall back on SDK's libffi.
+#    extra_compile_args += ['-iwithsysroot/usr/include/ffi']
 
 if 'freebsd' in sys.platform:
     include_dirs.append('/usr/local/include')
